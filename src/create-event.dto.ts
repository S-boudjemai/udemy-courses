export class CreateEventDto {
  // Dans ce fichier, nous avons créé une classe DTO afin de définir la structure des données que nous attendons pour créer un événement.
  name: string;
  description: string;
  when: string;
  address: string;
}
